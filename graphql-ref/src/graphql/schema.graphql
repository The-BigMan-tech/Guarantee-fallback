type CreateUserResponse {
    message:String!
    data:User!
}
type User {
    id: ID!
    name: String!
    email: String!
}
type Query {
    user(id: ID!): User
    allUsers: [User]
}
type Mutation {
    createUser(name:String!,email:String!):CreateUserResponse
}